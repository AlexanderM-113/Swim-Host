{
  "name": "workspace",
  "version": "0.0.0",
  "license": "UNLICENSED",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "electron:build-main": "pnpm --filter @workspace/electron run build",
    "electron:build-app": "pnpm --filter @workspace/swim-manager run build",
    "electron:build-api": "pnpm --filter @workspace/api-server run build",
    "electron:build-all": "pnpm run electron:build-api && pnpm run electron:build-app && pnpm run electron:build-main",
    "electron:pack": "pnpm run electron:build-all && electron-builder --config electron-builder.yml",
    "electron:dev": "pnpm run electron:build-main && electron electron/dist/main.js",
    "gen-license": "npx tsx scripts/gen-license-key.ts"
  },
  "private": true,
  "devDependencies": {
    "electron": "^39.8.5",
    "electron-builder": "^25.1.8",
    "prettier": "^3.8.3",
    "typescript": "~5.9.3"
  }
}
